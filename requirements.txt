# Core dependencies
# Note: torch is installed FIRST in build.sh using CPU-only version
# Versions pinned for stable deployment (Dec 2024)
torch==2.2.2  # Stable version with CPU support
torch-geometric==2.3.1
transformers==4.46.3
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2

# API and web server
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
pydantic==2.10.4
python-multipart==0.0.20

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# Data processing
scipy==1.14.1
networkx==3.4.2

# Utilities
python-dotenv==1.0.1
pyyaml==6.0.2
tqdm==4.67.1
# matplotlib and seaborn removed for deployment (not used in production code)
# matplotlib>=3.8.0
# seaborn>=0.13.0

# Optional: for Kaggle dataset download (not needed for deployment)
# kaggle>=1.5.16

